/** Must match server `ANDROID_NOTIFICATION_CHANNEL_ID` and `setNotificationChannelAsync` in the app. */
export const ANDROID_DEFAULT_NOTIFICATION_CHANNEL_ID = 'default';

/** Must match server `MESSAGE_REPLY_PUSH_CATEGORY_ID` in helpers/notifications. */
export const MESSAGE_REPLY_PUSH_CATEGORY_ID = 'message_reply';
export const MESSAGE_PUSH_REPLY_ACTION_ID = 'reply_message';
